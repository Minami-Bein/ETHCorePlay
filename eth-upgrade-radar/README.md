# Eth Upgrade Radar

Monorepo MVP scaffold for Ethereum upgrade monitoring.

## Quick start

```bash
cd eth-upgrade-radar
cp .env.example .env
docker compose up -d
```

Then start apps separately:

```bash
npm run dev:web
npm run dev:api
npm run dev:worker
npm run dev:ai
```
